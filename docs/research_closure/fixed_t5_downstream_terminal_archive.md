# Fixed T5 Downstream Terminal Archive

## Status

`Phase 0B = CLOSED`

Close reason: `trigger audit completed FAIL; research line archived`

This archive does not reopen Phase 0B and does not modify `docs/phase0b/05_phase0b_entry_check.md`.

## Frozen Facts

1. Phase 0A five-branch status is frozen as Branch 1 = `NO`, Branch 2 = `NO`, Branch 3 = `YES`, Branch 4 = `NO`, Branch 5 = `NO`.
2. Trigger, override, hard switch, and MoE routes are terminated.
3. `fixed T5 + downstream k + tail family` failed.
4. `Option 2A joint (mu, sigma) minimum falsification` failed.
5. `T5 rank + EGARCH scale hybrid` failed.
6. The strongest current conclusion is: fixed-T5 low-DOF downstream patching is exhausted.

## Terminal Scope

Closed methods include:

- any method using fixed T5 sigma, z, or rank output as the object being repaired;
- any trigger, override, hard switch, or MoE architecture;
- any rank-scale hybrid parameter expansion;
- any continuation of downstream scalar scale or tail repair around T5 output.

## Permitted New-Line Boundary

Future research must be independent of fixed T5 output. A valid new line may model the target return directly, model direct conditional quantiles directly, or decouple direction and scale heads from non-T5 features.
